services:
  - type: web
    name: washsetu-server
    runtime: node
    plan: free
    buildCommand: npm install && cd server && npm install && npm run prisma:generate
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 10000
